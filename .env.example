# Written automatically by `npx convex dev` for local development.
# On Vercel, set this to the production Convex deployment URL.
VITE_CONVEX_URL=

# The Gemini key is NOT set here — it lives in Convex:
#   npx convex env set GOOGLE_API_KEY <key>
