# Production Convex deployment (public client config — safe to commit).
# `npm run build` / Vercel use this; `npm run dev` uses .env.local (dev deployment).
VITE_CONVEX_URL=https://zealous-duck-145.eu-west-1.convex.cloud
